{
  "name": "symbiote",
  "version": "1.0.0",
  "description": "Self-Organizing Trading Agent Collective in DeFi",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:arbitrum-sepolia": "hardhat run scripts/deploy.js --network arbitrumSepolia",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "keywords": ["defi", "trading-agents", "ethereum"],
  "author": "Jacob",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.1.1"
  }
}
